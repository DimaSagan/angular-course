import { RateClassPipe } from './rate-class.pipe';

describe('RateClassPipe', () => {
  it('create an instance', () => {
    const pipe = new RateClassPipe();
    expect(pipe).toBeTruthy();
  });
});
